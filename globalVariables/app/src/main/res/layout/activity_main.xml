<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.chenye.globalvariables.MainActivity">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="全局变量Button"
        android:id="@+id/button"
        android:onClick="clickButton"
        />

    <Button
        android:id="@+id/ClipBoardButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button"
        android:onClick="ClickClipBoardButton"
        android:text="剪贴板Button" />
    <Button
        android:id="@+id/staticButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="静态变量button"
        android:onClick="ClickStaticButton"
        android:layout_below="@+id/ClipBoardButton"/>

</RelativeLayout>
